///// This template has to be copypasted on top of each NanoUI that supports modular computers
///// Unfortunately, i didn't find better way to do it. If author of NanoUI reads this, i'd
///// appreciate any suggestions on how to append this on top of multiple templates automatically.

{{if data.PC_hasheader}}
	<div style="float: left">
		<div class='item'>
			<table><tr>
			{{if data.PC_batteryicon}}
				<td><img src='{{:data.PC_batteryicon}}'>
			{{/if}}
			{{if data.PC_batterypercent}}
				<td><b>{{:data.PC_batterypercent}}</b>
			{{/if}}
			{{if data.PC_ntneticon}}
				<td><img src='{{:data.PC_ntneticon}}'>
			{{/if}}
			{{if data.PC_apclinkicon}}
				<td><img src='{{:data.PC_apclinkicon}}'>
			{{/if}}
			{{if data.PC_stationtime}}
				<td><b>{{:data.PC_stationtime}}<b>
			{{/if}}
			</table>
		</div>
	</div>
	<div style="float: right">
		<table><tr>
			<td>{{:helper.link('Shutdown', 'icon-power', {'PC_shutdown' : 1})}}
			<td>{{:helper.link('Exit Program', 'icon-close', {'PC_exit' : 1})}}
		</table>
	</div>
	<div style="clear: both">
<hr>
{{/if}}