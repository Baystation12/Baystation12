author: SierraKomodo
delete-after: true
changes:
  - admin: Var-editing the `health_max`, `health_current`, and `health_dead` vars
      is now properly sanitized and will call the relevant proc calls automagically.
      Beware setting health to 0 on anything that qdels itself when dead.
